옷장 메인 화면 재설계 — 목업 스펙 정합화 + 그룹형 아키텍처 전환     │
│                                                                     │
│ Context                                                             │
│                                                                     │
│ Task 7(옷장 메인)은 Visual Review 없이 추상적 UX 스펙 문서만 보고 구현됐다. 사용자가 실제 목업(사진)을 상세 스펙으로 옮겨 브리핑을 요청했고, 대조 결과 현재 구현은 레이아웃 세부, 그룹형 아키텍처(00_페이지타입정의.md L3), 다수 인터랙션(선택모드/삭제/검색/제스처)이 목업·디자인 문서와 크게 다름을 확인했다. 이번 작업은 그 간극을 스펙대로 메우는 재설계다.    │
│                                                                     │
│ 확정된 방향 (사용자 확인 완료):                                     │
│ - 그룹형(계절 그룹 타일 오버뷰 → 드릴다운) 정식 전환 — 이번 스코프 포함                                                                │
│ - Typography Pretendard 단일화 (KoPubDotum 제거) — Brand Guide Pass 3 확정 겸함                                                         │
│ - 정렬(sort) 실제 기능은 별도 Task로 미룸 — 아이콘 no-op 유지       │
│ - 선택모드/삭제 플로우는 별도 Task — 단 이번 설계는 그 확장을 막지 않게 고려                                                           │
│ - 사계절(allSeason) 폐기, 기존 데이터는 봄가을(간절기 리네임)로 재태깅                                                              │
│ - "후술 추가사항" 제외하고는 스펙대로 — Flutter 내장 위젯/기능 적극 재사용, 새 패키지 추가 지양                                         │
│                                                                     │
│ ---                                                                 │
│ 0. 정책 보완 — Development 파이프라인에 "설계 우선" 게이트 추가     │
│                                                                     │
│ 발견: Design 워크플로우는 Hi-Fi Sample→Visual Review 같은 명시적 설계 검증 단계가 있는데, Development 파이프라인(workflow_project/05_Standard Pipeline.md)은 L/XL(신규 화면)도 PM → Worker → Review → Tester → ...로 바로 구현부터 들어간다 — "어떻게 만들지"를 확정하는 단계가 없다. Task 7이 목업과 크게 어긋난 근본 원인.                                              │
│                                                                     │
│ 수정: .claude/policies/Workflow_Project.md §7 Change Impact 아래 신설:                                                               │
│                                                                     │
│ ▎ 7.1 Architecture/Design Pass Required for L/XL Screen-Level Frontend Tasks                                                    │
│ ▎                                                                   │
│ ▎ UI/Screen 신규 화면 또는 전면 재설계(Task Size L/XL)는 Worker 스폰 전에 아키텍처/설계 검토(데이터 흐름 구조, provider/state 모양, 신규 컴포넌트 경계, Design 문서 — UX/Interaction/Layout/Accessibility Principles, Component Strategy — 와의 정합성)를 먼저 글로 정리한다(plan/spec 문서로). §7 Change Impact("어떤 파일이 영향받는가")만으로는 대체되지 않는다 — 그건 범위 판단이고 이건 "어떻게" 구현할지의 설계다.      │
│                                                                     │
│ Backend/릴리즈테스트/배포 단계는 이 프로젝트에 아직 존재하지 않음(mock 데이터 단계) — 지금 정책을 미리 쓰지 않고, 해당 워크플로우가 실제로 생길 때 같은 원칙(설계 우선)을 적용하도록 남겨둔다.                                                           │
│                                                                     │
│ Decision.md에 이 정책 변경 기록 추가.                               │
│                                                                     │
│ ---                                                                 │
│ 1. Season enum 개편 (Layer=Data/Architecture, Decision+Implementation)                                            │
│                                                                     │
│ - lib/models/enums.dart: Season enum을 summer, winter, springFall(3종)로 축소. allSeason 제거. 라벨: 여름/겨울/봄가을.     │
│ - 순서: enum 선언 순서 자체를 springFall, summer, winter로 재배치(Season.values 순회 시 봄가을→여름→겨울 순서가 나오도록) — UI의 "전체보기"는 null이라 enum 값이 아니므로 드롭다운 코드에서 [null, ...Season.values]로 조합.                                    │
│ - lib/mock/mock_data.dart: 기존 Season.allSeason 아이템 4개(c02/c03/c05/c08) → Season.springFall로 재태깅(사용자 확정).     │
│ - docs/reference/plan/03_화면별UX명세서/_공통 규칙.md 74행 "계절 | 여름 → 겨울 → 간절기 → 사계절" → "봄가을 → 여름 → 겨울"로 수정(이 표가 정렬 기본순서의 Source of Truth).                              │
│ - docs/history/Decision.md에 새 항목(맨 위, append-only 원칙 — 기존 "Season enum 4종 확정" 항목은 그대로 두고 새 항목이 대체함을 명시)으로 이번 번복 기록: 무엇을·왜·기존 결정과의 관계.             │
│ - 영향 파일 전수 확인: closet_providers.dart, composition 관련 mock(season: Season.transitional 사용하는 2곳), 기존 integration_test/closet_main_screen_test.dart(겨울 대신 봄가을이 0개 계절인지 mock 데이터로 재확인 필요 — 값이 바뀌므로 "빈 계절" 테스트의 전제 데이터가 달라질 수 있음).                             │
│                                                                     │
│ 2. Design Tokens 확정/신설 (Layer=UI/Screen, Decision — Visual Review 겸함)                                                        │
│                                                                     │
│ - Typography: lib/theme/app_typography.dart의 bodyLarge/Medium/Small을 KoPubDotum→Pretendard로 교체. pubspec.yaml의 KoPubDotum 폰트 등록은 다른 화면에서 참조 중인지 확인 후(안 쓰면) 제거 검토(사용 중이면 등록은 유지하되 옷장 화면만 Pretendard 사용).                                                   │
│ - AppRadius 신설 (lib/theme/app_spacing.dart에 병기하거나 새 파일 app_radius.dart): 목업 값 중 실제 앱에 적용 가능한 것만 채택 — sm=16(드롭다운 패널), pill=100(필 버튼). "폰 프레임 44px"는 목업 프레젠테이션용 목업-디바이스 테두리 표현이지 앱이 그릴 요소가 아니므로 채택하지 않음(OS가 실제 기기 프레임을 그림).               │
│ - Glass 헤더 opacity 비교대조 완료: lib/widgets/overlay_header.dart의 현재 alpha: 0.7 → 목업 값 0.38로 변경. 보더/그림자는 목업에 "얇은 화이트 보더 + soft shadow"로만 서술되어 정확한 수치가 없음 — Design Tokens에 Elevation 값 자체가 미정(리서치 확인)이므로, 합리적인 기본값(Border.all(color: Colors.white.withValues(alpha: 0.2)), 약한 BoxShadow)을 잠정 채택하고 AppSemanticColors/토큰으로 등록(매직넘버 방지) — Decision.md에 "잠정값, 추후 실측 확정 필요"로 기록.                 │
│ - Motion 값 신설: Design Tokens에 Motion.standard라는 역할명만 있고 값이 없음(리서치 확인) — 이 화면은 UX P1(속도 우선 유틸리티 경로)이므로 짧게: AppMotion.fast = 200ms, 검색 필드 확장은 사용자가 준 스펙값 250–350ms 그대로(예: 300ms) 별도 상수로. AppMotion 클래스 신설, 매직넘버로 흩뿌리지 않음.                              │
│ - AppDensity 컬럼 수: 기존 min=1/mid=3/max=5 유지(이미 3단계 요구사항 충족, 변경 불필요) — 단 순환 방향 수정: 목업은 "그리드 컬럼 5→3→1"이 탭 진행 방향이므로, 현재 오름차순(3→5→1) 대신 내림차순으로 순환하도록 closet_main_screen.dart의 밀도 토글 로직 수정. 아이콘도 밀도 단계별로 다른 아이콘(예: Icons.view_comfy/Icons.grid_view/Icons.crop_square 또는 유사 Material 아이콘)으로 교체해 고정 아이콘 문제 해소.                  │
│                                                                     │
│ 3. 아키텍처 — 그룹형(Grouped) 드릴다운 전환 (Layer=UI/Screen, Decision+Implementation)                                            │
│                                                                     │
│ 핵심 설계 결정: 분류 기준은 이번엔 "계절" 하나만 지원하되, 향후 다른 기준(옷 종류/색상/착용빈도 등, _공통 규칙.md 정렬기준표)이 추가돼도 구조가 깨지지 않게 기준을 열거형으로 감싼다.               │
│                                                                     │
│ // lib/models/enums.dart 또는 별도 파일                             │
│ enum ClosetGroupCriterion { season } // 향후 category, color, wearFrequency 등 추가 가능                                          │
│                                                                     │
│ // lib/providers/closet_providers.dart                              │
│ final closetGroupCriterionProvider = StateProvider<ClosetGroupCriterion>((ref) => ClosetGroupCriterion.season);                                       │
│                                                                     │
│ /// null = 그룹 오버뷰(타일), non-null = 해당 그룹으로 드릴인. "전체보기"는 별도 sentinel이 필요                                   │
│ /// (그룹 안 하고 전부 플랫으로 보는 상태) — 아래 3-상태 모델 참고. │
│ enum ClosetViewMode { groupOverview, allFlat, drilledInto }         │
│ final closetViewModeProvider = StateProvider<ClosetViewMode>((ref) => ClosetViewMode.groupOverview);                                   │
│ final closetDrilledSeasonProvider = StateProvider<Season?>((ref) => null); // drilledInto일 때만 유효                                   │
│                                                                     │
│ final closetGroupedOverviewProvider = Provider<Map<Season, List<ClothingItem>>>((ref) { ... }); // 그룹 오버뷰 타일용          │
│ final closetVisibleItemsProvider = Provider<List<ClothingItem>>((ref) { ... }); // 현재 모드에 따라 allFlat 또는 drilledInto 리스트 반환                                │
│                                                                     │
│ 내비게이션 모델 (01_옷장.md "분류 그룹 클릭 → 목록 진입", _공통 규칙.md 뒤로가기 원칙 재사용):                                      │
│ - 화면 진입 기본 상태 = groupOverview (봄가을/여름/겨울 그룹 타일, 각 타일에 개수+대표 썸네일).                                        │
│ - 그룹 타일 탭 → drilledInto + 해당 시즌으로 전이(스택 push 개념).  │
│ - 계절 드롭다운에서 "전체보기" 선택 → allFlat으로 직접 전이(그룹 건너뛰고 전부 플랫).                                                │
│ - 계절 드롭다운에서 특정 계절 선택 → drilledInto 직접 전이(오버뷰를 거치지 않고 바로 감).                                               │
│ - 좌하단 원형 뒤로가기 버튼: drilledInto/allFlat 상태에서만 노출, 탭하면 groupOverview로 복귀(_공통 규칙.md: "이동 스택이 있을 때만 노출, 한 단계씩 이동"과 정합).                                      │
│ - 이 해석은 목업 스크린샷(드릴인된 상태로 추정)과 문서 텍스트를 조합한 설계 판단 — Review 단계에서 실제 목업 이미지와 한 번 더 대조 권장.                                                               │
│                                                                     │
│ 4. 화면 레이아웃/네비게이션 재구축 (Layer=UI/Screen, Implementation)                                                     │
│                                                                     │
│ lib/screens/closet_main_screen.dart 대폭 재작성 + 신규 위젯:        │
│                                                                     │
│ - 최상단 카테고리 드롭다운("옷장▾"): 옷장/코디/스타일일지 3항목 + 활성 도트. 선택 시 context.go(AppRoute.compositionMain) / context.go(AppRoute.styleLogMain) — _공통 규칙.md "선택 시 해당 카테고리는 항상 루트에서 시작"에 따라 go(스택 대체), push 아님.     │
│ - 우측 "선택"/"취소" 토글: 버튼만 배치(선택모드 진입 로직은 Out of Scope — 아래 §7 참고), 탭해도 당장은 아무 동작 안 함(no-op, 정렬 아이콘과 동일 취급) — 단 버튼 자체와 자리는 마련.                   │
│ - 두 번째 툴바 행: 계절 세그먼트 필(그룹 오버뷰/드릴인/전체보기 상태 반영) + 밀도 토글(§2 순환 수정 반영) + 우측 원형 버튼(§5 Expandable Search로 교체, 더 이상 placeholder 아님).                │
│ - 하단 좌측 원형 뒤로가기: §3 내비게이션 모델에 따라 조건부 노출.   │
│ - 하단 우측 FAB: 탭 시 "한 장 추가하기"/"여러 장 추가하기" 2옵션이 위로 펼쳐짐 + FAB 자체 45도 회전 애니메이션(AnimatedRotation, AppMotion.fast). 옵션 라벨은 현재 뷰 상태(전체보기 vs 특정 계절)에 따라 "이 분류에 ~"로 전환(01_옷장.md 반영). 두 옵션 탭 시 실제 후속 플로우(이미지 업로드 등)는 Out of Scope — 지금은 AppRoute.closetAdd로 이동만.                                        │
│ - 스크롤 페이드 마스크: ShaderMask + LinearGradient(alpha 그라디언트)로 그리드 상단(헤더 겹침 구간)을 페이드 처리. 스크롤 컨트롤러 리스너로 필요시 마스크 강도 조정(정적 마스크로 시작, 스크롤값 기반 동적 조정은 1차 구현 이후 다듬어도 됨).               │
│ - 배경: Container decoration에 LinearGradient(#F7F6F3→#EDE9E1) + 우상단 RadialGradient 세이지(#C5D3C7) 틴트 겹치기.                  │
│ - 장식용 상태바: 릴리즈 빌드에 포함하지 않음. kDebugMode(Flutter 내장 상수, foundation.dart)로 감싸 디버그 전용으로만 렌더링. 조건부 컴파일이 여의치 않으면(예: 위젯 트리 구조상 부담) 상태바 "영역"만 SizedBox로 예약하고 실제 시계/신호 아이콘 등 디자인 요소는 아예 넣지 않는 쪽으로 폴백.                                              │
│ - 홈 인디케이터 바: 실제 기기에서는 OS가 그리는 영역(SafeArea/MediaQuery.padding.bottom)이라 앱이 직접 그리지 않음 — SafeArea 적용으로 충분, 별도 위젯 불필요.                         │
│ - HUD 스크롤바: Flutter 내장 Scrollbar/RawScrollbar 위젯 재사용(새 패키지 불필요). thumbVisibility: false, trackVisibility: false(기본 auto-hide), thickness를 얇게, radius는 pill형, 상하 마진 동일 + 화면 전체 길이가 되도록 RawScrollbar의 padding/직접 커스텀 필요할 수 있음(기본 Material Scrollbar가 이 마진 요구사항을 못 맞추면 RawScrollbar + 커스텀 thumbColor/fadeDuration으로 대체). Fade-in 빠르게(~100ms) / ~0.9초 유지 후 fade-out은 RawScrollbar의 fadeDuration/timeToFade 파라미터로 직접 대응 가능(Flutter SDK가 이미 지원하는 파라미터 — 재구현 불필요).                            │
│ - 타일 라벨: _공통 규칙.md #73 "옷 종류" 기준 → SelectableGalleryTile의 라벨을 item.name(상품명) 대신 item.category.label(옷 종류, 예: "상의"/"하의")로 변경. 모노스페이스체 요구는 목업 원문이나, 프로젝트 폰트 정책(Pretendard 단일화)과 충돌 — Pretendard에 고정폭 스타일이 없으므로 fontFamily: 'monospace'(Flutter 내장 fallback) 단독 적용 또는 이 요구사항 자체를 재확인 필요(사소한 항목이라 우선 Pretendard 유지, 필요시 후속 조정으로 남김).                                                │
│ - 미완성 배지 위치: 좌상단 유지(사용자 확정, 현재 코드와 이미 일치 — 변경 없음).                                                       │
│                                                                     │
│ 5. 신규 컴포넌트 (Layer=UI/Screen, Implementation) — Component Strategy에 신규 등재 필요                                           │
│                                                                     │
│ 5.1 ExpandableSearchField (원형 ◎ → 캡슐형 검색창)                  │
│                                                                     │
│ 새 위젯 lib/widgets/expandable_search_field.dart:                   │
│ - 단일 위젯이 원형↔캡슐형으로 모핑(사용자 스펙: 새 TextField 생성 방식 금지, 위젯 하나가 변형).                                       │
│ - AnimatedContainer로 width/BorderRadius 애니메이션(250–350ms, 예: 300ms = AppMotion 상수).                                            │
│ - 검색 아이콘은 항상 마운트 유지, leading 위치 고정 — 오른쪽으로만 확장.                                                               │
│ - FocusNode + TextEditingController로 포커스/키보드 제어, 확장 완료 후 FocusNode.requestFocus().                                        │
│ - 축소 조건: 텍스트 비어있고 포커스 잃으면(또는 뒤로가기) 자동 축소. 텍스트 있으면 유지.                                           │
│ - 컨텍스트 보존(리서치에서 발견한 UX P7/Interaction I8): 검색어 상태는 위젯 로컬이 아니라 Provider(closetSearchQueryProvider)에 둬서, 다른 오버레이/네비게이션 왕복에도 유지되게 한다.              │
│ - 실제 검색 로직(텍스트로 아이템 필터링)은 이번 Task엔 미포함 가능 — UI 모핑 애니메이션과 포커스 흐름까지만이 핵심 요구사항이었음(사용자 스펙에 필터링 로직 언급 없음). Provider는 미리 만들어두되 소비하는 필터링은 후속으로 남겨도 됨 — Worker 판단. │
│                                                                     │
│ 5.2 HUD 스타일 스크롤바 wrapper                                     │
│                                                                     │
│ §4에서 설명한 RawScrollbar 커스터마이징을 lib/widgets/hud_scrollbar.dart로 감싸 재사용 가능하게 — 다른 갤러리 화면(코디/스타일일지)에도 나중에 그대로 쓸 수 있게.                 │
│                                                                     │
│ 두 컴포넌트 모두 완료 후 docs/reference/design/00_DesignPrinciples/06_Component Strategy.md에 C13/C14로 신규 등재 — 문서화 컨벤션(documentation-conventions 스킬) 대상.                        │
│                                                                     │
│ 6. 제스처 통합 (Layer=UI/Screen, Implementation) — Accessibility A11 대응                                                            │
│                                                                     │
│ - 핀치: GestureDetector.onScaleUpdate로 그리드에 걸어 연속 피드백을 주되, onScaleEnd에서 가장 가까운 3단계(min/mid/max) 중 하나로 스냅(연속 자유 크기조절 아님 — Layout L4 "정확히 3단계" 준수).      │
│ - 롱프레스: 다중 선택 모드 진입(§7에서 실제 모드 자체는 Out of Scope이므로, 이번엔 롱프레스 핸들러를 "다중 선택 진입 지점"으로 표시만 해두고 실제 모드 전환은 구현하지 않음 — 또는 TODO 주석 + 향후 확장 지점으로 명시).                                           │
│ - 충돌 해소(A11): 두 번째 터치 포인트(핀치 시작) 감지 시 진행 중이던 롱프레스 타이머를 명시적으로 취소. GestureDetector의 onScaleStart가 발동하면 롱프레스 인식을 억제하는 순서로 배치.       │
│ - 탭: 그룹 오버뷰에서는 그룹 진입, 
│                                                                     │
│ - 선택모드/삭제 플로우: 하단 삭제바, 체크서클 실제 렌더링, 삭제→휴지통 이동, undo 토스트(리서치에서 확인된 C7 UndoableActionToast 패턴 — 전역/영속 상태로 화면 전환에도 살아남아야 함). 확장성을 위해 이번 설계에서 미리 고려한 것: ClosetItemsNotifier.softDelete는 이미 존재(Task 1-6 산출물), provider 구조가 이미 Riverpod 전역 상태라 향후 선택모드 Provider 추가가 자연스러움. SelectableGalleryTile의 selected prop도 이미 있음(시각 요소만 비어있음). 즉 선택모드를 나중에 얹을 때 지금 만드는 구조를 갈아엎을 필요는 없을 것으로 판단됨.                   │
│ - 정렬 실제 기능: 아이콘 no-op 유지(사용자 확정).                   │
│ - "한 장/여러 장 추가"의 후속 업로드 플로우: FAB 확장 메뉴까지만, 탭 이후 실제 편집기 플로우는 Task 11(옷 추가하기) 범위.             │
│ - 검색 실제 필터링 로직: 위젯의 모핑/포커스 흐름만, 텍스트 매칭은 후속(§5.1 참고).                                                    │
│ - 글래스 헤더 보더/그림자 정확한 수치: 잠정값 채택(§2), 실측 확정은 추후.                                                               │
│                                                                     │
│ ---                                                                 │
│ Task 분해 (실행 순서)                                               │
│                                                                     │
│ 1. Season enum 개편 (Data/Architecture) — 다른 모든 작업의 전제, 가장 먼저.                                                          │
│ 2. Design Tokens 확정 (UI/Screen, Decision) — Typography/Radius/Motion/Glass opacity, §1 이후.                    │
│ 3. Provider 재구조화 (Data/Architecture) — §3의 그룹형 상태 모델.   │
│ 4. 화면 레이아웃 재구축 (UI/Screen, Implementation) — §4, 2·3에 의존.                                                               │
│ 5. ExpandableSearchField + HUD Scrollbar (UI/Screen, Implementation) — §5, 4와 병행 가능.                                │
│ 6. 제스처 통합 (UI/Screen, Implementation) — §6, 4·5 완료 후.       │
│ 7. Component Strategy 문서 등재 (문서화) — 5 완료 후.               │
│                                                                     │
│ 각 Task는 기존 하네스대로 Worker → Review → Tester 사이클(Layer=UI/Screen 또는 Data/Architecture에 맞는 Required Materials로 Task Manifest 작성 — .claude/policies/workflow_project/12_Role Information Access.md §12.4 참고).                                                        │
│                                                                     │
│ ---                                                                 │
│ Verification                                                        │
│                                                                     │
│ - flutter analyze 클린, 기존 integration_test/ 전체(개별 파일 단위 실행 — Windows desktop 툴링 특성) 통과.                             │
│ - closet_main_screen_test.dart의 "겨울 계절 = 빈 그리드" 테스트는 계절 값이 바뀌므로 재확인/수정 필요(어떤 계절이 실제로 0개인지 mock 데이터 재검토 후).                                                  │
│ - 그룹 오버뷰 → 드릴인 → 뒤로가기 → 오버뷰 복귀 왕복을 Tester가 실제로 구동 검증.                                                   │
│ - 접근성: A10(밀도 토글 히트영역 44×44), A6(타일 접근성 라벨에 옷 종류 라벨 반영), A11(핀치/롱프레스 충돌) — Review 체크리스트에 명시적으로 포함.                                                    │
│ - 검색 필드 확장/축소, 포커스 이동을 Tester가 위젯 트리 구동으로 검증.                                                               │
│                                                                     │
│ 부록 — 새 세션 vs 현재 세션                                         │
│                                                                     │
│ 이번 리서치(목업 대조, 디자인 문서 전수 검토)는 현재 세션에 이미 로드돼 있어 여기서 설계를 마무리하는 게 맞다. 다만 실행 단계(Task 1~7 디스패치)는 새 세션을 권장 — Worker/Review/Tester는 어차피 매번 새 서브에이전트로 스폰되므로(이 PM 세션의 대화 길이와 무관), 새 세션이 얻는 이득은 PM 자신의 누적 컨텍스트(Tester 하네스 구축기, 여러 차례의 git 병합 사투 등 이번 작업과 무관한 이력)를 반복해서 요약 인식하지 않아도 된다는 점. 이 플랜 파일 자체가 새 세션의 완전한 인계 자료가 되므로, 새 세션에서 "이 플랜대로 진행해줘"로 시작해도 손실이 없다.