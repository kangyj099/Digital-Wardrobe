<!--> 하네스(PM/Worker/Review 서브에이전트 + 훅) 구축 진행 상황. 세션이 clear돼도 여기 보고 이어서 하면 됨 <-->

## 완료

- [x] `.claude/policies/Workflow_Project.md` §3, §11, §12 갱신 (핸드오프 자동화 원칙, 역할별 자료 접근권 Layer×Stage 매핑)
- [x] `docs/history/TechnicalDebt.md` 생성
- [x] `docs/reference/` 하위 폴더 재분류 확인 (plan/design + 공통 Glossary.md; 정책 문서는 이후 `.claude/policies/`로 별도 이전됨)
- [x] `CLAUDE.md`에 "하네스 운영 원칙 (이 세션 = PM)" 섹션 추가 — 체크포인트 1~4, Layer×Stage 태깅, 워커 인스턴스 수명 판단, 스코프 확장 규칙
- [x] `docs/work/BACKLOG.md` 생성 — 프로젝트 전체 현재 상태 스냅샷
- [x] `.claude/agents/worker.md` 서브에이전트 정의 작성
- [x] `.claude/agents/review.md` 서브에이전트 정의 작성
- [x] `.claude/settings.json` + `.claude/hooks/block-git-commit.sh` — git commit 게이팅 훅 추가, `git commit`/`git status` 입력으로 테스트 완료 (차단/통과 정상 동작 확인)

- [x] 더미 작업으로 실제 worker→review 사이클 한 번 돌려보기 (서브에이전트 스폰이 실제로 동작하는지, 자료 스코프가 의도대로 제한되는지 확인)
  - 세션 재시작 후 재시도하니 `worker`/`review` 서브에이전트 타입이 정상 인식됨.
  - 더미 태스크(Logic/Feature × Implementation, 착용 카운트 표시 포맷터): Worker → Review 1회전으로 완료, Review는 P0/P1 없음 판정. §12.1/12.2에 따라 Worker에는 스펙 자료+기존 코드, Review에는 Worker handoff+동일 판단기준 문서(+Decision/TechnicalDebt)만 넘기는 방식이 실제로 동작함을 확인.
  - 산출물(lib/utils/wear_count_label.dart, test/wear_count_label_test.dart)은 테스트용이라 커밋 없이 삭제함.

## 진행 중 / 남은 일

(없음 — 하네스 기본 구축 + 1회 사이클 검증 완료)

## 참고

- 체크포인트 5(필수 확인 항목의 "다음부터 자동승인" 버튼 제거)는 드랍됨 — 권한 시스템 레벨에서 불가능 확인됨. 다시 시도할 필요 없음.
- 관련 결정: `Decision.md`의 "역할별 자료 접근권 원칙 추가", "Claude Projects 사용 중단..." 항목.
- **자료 스코프는 소프트 가드**: `.claude/agents/*.md`의 `tools:` 필드(Read/Edit/Write/Bash 등 툴 종류 제한)는 하드 경계지만, "어떤 파일/경로를 보는지"는 하드 경계가 아니라 PM이 매번 프롬프트에 적어주는 지시에 의존하는 소프트 가드임 (Claude Code 자체에 서브에이전트별 경로 스코핑 기능 없음, settings.json의 permissions 규칙도 세션 전체에 적용되고 서브에이전트별로 분리되지 않음). 더 강한 강제가 필요하면 `block-git-commit.sh`처럼 PreToolUse 훅으로 파일 경로를 검증하는 방법이 있음 — 필요해지면 그때 추가.
